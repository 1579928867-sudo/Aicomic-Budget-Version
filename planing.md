# AI 漫剧生成助手 · 多 Agent 协作框架

## 项目目标
输入小说章节文本 → 自动生成剧本（含分镜） → 提取人物与场景并固化多视图 → 豆包生成漫剧视频片段 → 拼接合成带配音、字幕、运镜的完整漫剧，全程保证视觉一致性。

## 核心架构
系统由 **6 个专职 Agent** + **统一知识库** 组成，由协调 Agent 串行调度。

### Agent 定义与分工

| Agent | 职责 | 输入 | 输出 |
|-------|------|------|------|
| **编剧 Agent** | 将章节改编为剧本（天然含分镜），从剧本中提取人物和场景列表 | 章节文本 | 剧本 JSON（含分镜）、人物列表、场景列表 |
| **人物 Agent** | 为每个人物生成固化外貌描述，调用豆包生成三视图并保存 | 人物名称 + 剧本上下文 | 人物卡（外貌描述 + 三视图路径） |
| **场景 Agent** | 为每个场景生成固化场景描述，调用豆包生成多视图并保存 | 场景名称 + 剧本上下文 | 场景卡（描述 + 多视图路径） |
| **视觉 Agent** | 根据剧本分镜 + 人物/场景视觉资产，调用豆包生成 3D 漫剧视频片段（≤10s） | 分镜 + 人物三视图 + 场景多视图 + 对话 | 视频片段文件路径列表 |
| **合成 Agent** | 拼接片段、添加运镜衔接、生成配音、烧录字幕、输出完整视频 | 视频片段 + 剧本分镜 + 配音文本 | 最终 MP4 |
| **协调 Agent** | 流程编排、异常处理、进度反馈、时长控制校验、断点续跑 | 用户指令（novel_id, 章节文本） | 最终视频路径 |

## 剧本优先的工作流
```mermaid
graph TD
    A[章节文本] --> B[编剧 Agent: 生成剧本+分镜]
    B --> C{提取人物列表}
    B --> D{提取场景列表}
    C --> E[人物 Agent: 逐个生成外貌描述 + 三视图]
    D --> F[场景 Agent: 逐个生成描述 + 多视图]
    E --> G[视觉 Agent: 逐镜生成视频片段 (≤10s)]
    F --> G
    G --> H[合成 Agent: 拼接+配音+字幕]
    H --> I[最终漫剧视频]
帮我制作成一个md文件